2696. Minimum String Length After Removing Substrings

https://leetcode.com/problems/minimum-string-length-after-removing-substrings/submissions/1889534261/

class Solution {
    public int minLength(String s) {
        Stack<Character> st = new Stack<>();
        st.push(s.charAt(0));
        for(int i=1;i<s.length();i++){
            if(st.isEmpty()){
                st.push(s.charAt(i));
            }
            else if(st.peek()=='A' && s.charAt(i)=='B'){
                st.pop();
            }
            else if(st.peek()=='C' && s.charAt(i)=='D'){
                st.pop();
            }
            else{
                st.push(s.charAt(i));
            }
        }
        return st.size();
    }
}
